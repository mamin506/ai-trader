# AI Trader Default Configuration
# This file contains minimal configuration needed for current implementation

# Project metadata
project:
  name: "ai-trader"
  version: "0.1.0"
  mode: "backtest"  # backtest, paper, or live

# Logging configuration
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
